# 🐳 Docker Deployment для ЦВС

## Описание

Docker-конфигурация для развертывания Django-приложения ЦВС с использованием:
- **Django** - приложение на Gunicorn
- **Nginx** - веб-сервер для статики и проксирования
- **PostgreSQL** - база данных (опционально)

---

## 📋 Структура файлов

```
cvs/
├── Dockerfile                  # Образ Django приложения
├── docker-compose.yml          # Оркестрация контейнеров
├── .dockerignore              # Исключения при сборке образа
├── .env.example               # Пример переменных окружения
├── nginx/
│   ├── nginx.conf             # Основная конфигурация nginx
│   └── conf.d/
│       └── cvs.conf          # Конфигурация сайта
└── requirements.txt           # Python зависимости
```

---

## 🚀 Быстрый старт

### 1. Подготовка

Убедитесь, что установлены:
- Docker (версия 20.10+)
- Docker Compose (версия 2.0+)

### 2. Настройка переменных окружения

Скопируйте пример и отредактируйте:
```bash
cp .env.example .env
```

**Важно!** Измените в `.env`:
- `SECRET_KEY` - генерируйте длинный случайный ключ
- `ALLOWED_HOSTS` - укажите свой домен
- Пароли для базы данных

### 3. Добавьте gunicorn в requirements.txt

Проверьте, что в `requirements.txt` есть:
```
gunicorn>=21.2.0
psycopg2-binary>=2.9.9  # для PostgreSQL
```

Если нет - добавьте эти строки.

### 4. Запуск

#### Сборка и запуск всех контейнеров:
```bash
docker-compose up -d --build
```

#### Применение миграций:
```bash
docker-compose exec web python manage.py migrate
```

#### Создание суперпользователя:
```bash
docker-compose exec web python manage.py createsuperuser
```

#### Проверка статуса:
```bash
docker-compose ps
```

### 5. Доступ к приложению

Откройте в браузере:
```
http://localhost
```

Админ-панель:
```
http://localhost/admin
```

---

## 📦 Основные команды

### Управление контейнерами

```bash
# Запуск всех сервисов
docker-compose up -d

# Остановка всех сервисов
docker-compose down

# Перезапуск сервиса
docker-compose restart web

# Просмотр логов
docker-compose logs -f web
docker-compose logs -f nginx

# Просмотр запущенных контейнеров
docker-compose ps
```

### Django команды

```bash
# Миграции
docker-compose exec web python manage.py makemigrations
docker-compose exec web python manage.py migrate

# Сбор статики
docker-compose exec web python manage.py collectstatic --noinput

# Создание суперпользователя
docker-compose exec web python manage.py createsuperuser

# Запуск shell
docker-compose exec web python manage.py shell
```

### База данных

```bash
# Вход в PostgreSQL
docker-compose exec db psql -U cvs_user -d cvs_db

# Бэкап базы данных
docker-compose exec db pg_dump -U cvs_user cvs_db > backup.sql

# Восстановление базы данных
cat backup.sql | docker-compose exec -T db psql -U cvs_user -d cvs_db
```

---

## 🔧 Настройка для продакшена

### 1. SSL/HTTPS (Let's Encrypt)

Установите certbot и получите сертификаты:

```bash
# На хост-машине
sudo apt-get update
sudo apt-get install certbot

# Получить сертификат
sudo certbot certonly --standalone -d your-domain.com
```

Раскомментируйте HTTPS секцию в `nginx/conf.d/cvs.conf` и укажите пути к сертификатам.

### 2. Переменные окружения

В production не забудьте:
- Сгенерировать надежный `SECRET_KEY`
- Установить `DEBUG=False`
- Указать реальный домен в `ALLOWED_HOSTS`
- Использовать сильные пароли БД

### 3. Volumes для данных

Volumes автоматически создаются для:
- `postgres_data` - данные PostgreSQL
- `static_volume` - статические файлы
- `media_volume` - загруженные файлы

### 4. Масштабирование

Увеличьте количество воркеров Gunicorn в `docker-compose.yml`:
```yaml
command: gunicorn --bind 0.0.0.0:8000 --workers 4 cvs.wsgi:application
```

Рекомендуется: `workers = 2 * CPU_cores + 1`

---

## 🐛 Решение проблем

### Контейнер не запускается

```bash
# Проверьте логи
docker-compose logs web

# Пересоберите образ
docker-compose down
docker-compose up -d --build
```

### Статика не отдается

```bash
# Пересоберите статику
docker-compose exec web python manage.py collectstatic --noinput --clear

# Проверьте права доступа
docker-compose exec web ls -la /app/staticfiles
```

### База данных не подключается

```bash
# Проверьте, что контейнер БД запущен
docker-compose ps db

# Проверьте логи БД
docker-compose logs db

# Проверьте переменные окружения
docker-compose exec web env | grep DATABASE
```

### Ошибка "migrations not applied"

```bash
# Примените миграции
docker-compose exec web python manage.py migrate
```

---

## 📊 Мониторинг

### Просмотр использования ресурсов

```bash
docker stats
```

### Проверка здоровья контейнеров

```bash
docker-compose ps
```

### Логи в реальном времени

```bash
# Все сервисы
docker-compose logs -f

# Конкретный сервис
docker-compose logs -f web
docker-compose logs -f nginx
docker-compose logs -f db
```

---

## 🔐 Безопасность

### Рекомендации:

1. **Никогда не коммитьте `.env` файл в Git**
2. **Используйте сильные пароли** для БД
3. **Регулярно обновляйте** Docker образы
4. **Настройте firewall** на сервере
5. **Используйте HTTPS** в продакшене
6. **Регулярно делайте бэкапы** БД

### Генерация SECRET_KEY

```python
python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
```

---

## 🔄 Обновление приложения

```bash
# 1. Получите новый код
git pull

# 2. Пересоберите контейнеры
docker-compose down
docker-compose up -d --build

# 3. Примените миграции
docker-compose exec web python manage.py migrate

# 4. Соберите статику
docker-compose exec web python manage.py collectstatic --noinput
```

---

## 📝 Дополнительные настройки

### Использование SQLite вместо PostgreSQL

Если хотите использовать SQLite (не рекомендуется для продакшена):

1. Закомментируйте сервис `db` в `docker-compose.yml`
2. Удалите `depends_on: - db` из сервиса `web`
3. Закомментируйте `DATABASE_URL` в переменных окружения
4. Убедитесь, что `db.sqlite3` монтируется как volume

### Настройка Email

Добавьте в `.env`:
```env
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password
```

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи: `docker-compose logs`
2. Убедитесь, что порты 80/443 свободны
3. Проверьте переменные окружения в `.env`
4. Пересоберите контейнеры: `docker-compose up -d --build`

---

## ✨ Готово!

Ваше приложение ЦВС развернуто в Docker с nginx! 🎉

Для продакшена не забудьте:
- ✅ Настроить SSL/HTTPS
- ✅ Изменить SECRET_KEY
- ✅ Настроить бэкапы БД
- ✅ Настроить firewall
- ✅ Установить мониторинг

